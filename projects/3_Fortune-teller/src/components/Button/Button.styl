.button {
  display: inline-block;
  min-width: 100px;
  padding: 10px 20px;
  border-radius: 10px;
  font-family: inherit;
  font-size: inherit;
  background-color: #ccc;
  cursor: pointer;

  &:hover {
    background-color: #aaa;
  }

  &:focus {
    outline: none;
  }
}
