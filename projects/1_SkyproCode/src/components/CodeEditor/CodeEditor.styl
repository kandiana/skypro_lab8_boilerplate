.code-editor {
  height: calc(100vh - 100px);
  width: 100%;

  & > .container {
    min-height: 200px;
    height: 90%;
    margin-bottom: 10px;
    border: 1px solid black;
    box-sizing: border-box;
    overflow: hidden;
  }
}
